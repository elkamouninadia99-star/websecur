import Foundation from "./Foundation";
import { Globe2, Shield, Users } from "lucide-react";
const stats = [["Built for growth", Globe2], ["Security-first", Shield], ["Human support", Users]] as const;
export default function About() { return <section id="about" className="section bg-[#081321]"><div className="container grid gap-8 lg:grid-cols-2 lg:gap-14"><Foundation /><div><span className="eyebrow">About ProGuard Web</span><h2 className="section-title">The clarity of a product team, with the care of a security partner.</h2><p className="section-copy">ProGuard Web helps organizations present their best work online while taking the reliability and security behind it seriously.</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{stats.map(([label, Icon]) => <div key={label} className="premium-card rounded-xl border border-white/10 bg-white/[.025] p-4"><Icon className="size-5 text-cyan-200"/><p className="mt-4 text-sm font-medium text-slate-200">{label}</p></div>)}</div></div></div></section>; }
